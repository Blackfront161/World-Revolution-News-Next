# Closed production Website package

Parent RELEASE-COMPLETION, explicit PO all-six completion. Delegation: erlaubt,
Slot3 existing website_knowledge_support Terra/high; no children. Start only
after Slot1 delivery-tool correction returns writer rights; Root remains the
other possible writer for disjoint update-controller/client files. Own exactly
new apps/website/tools/production-site-package.mjs and its .test.mjs plus own
matching SITE-PACKAGE-WRITER evidence/handoff. No existing source, App, build,
index, browser, provider, dependency or config mutation.

Existing staging builder is intentionally fixture-specific (21files/3test IDs),
so it stays unchanged. New prepareProductionWebsitePackage and CLI accept an
existing fresh production buildDirectory, absent outputDirectory, explicit
trustedWorkspaceRoot, sourceCommit, generatedAtUTC. Do not run a build, network
or Git from the tool. Strict CLI using node:util parseArgs or equivalent exact
grammar. Explicit real workspace/ancestor containment including Junctions for
input and new output. No deletes/moves/overwrite. Use owned fresh scratch and
exclusive writes; partial output may remain on failure but never report ready.

First freeze the current5f1145d build to own fresh test input before Root's next
build; report this promptly. Tests use that copy and current actual admitted
production public release, not old test-only IDs. Final artifact will be rebuilt
by Chief after all client work. Byte input inventory/hash binds the source.

## Reuse and exact public closure

Reuse collectShellManifest/buildOfflineShell, the production release disk loader,
and existing buildStagingSecurityHeaders only as a CSP/header constructor (drop
its staging-only x-robots-tag; never run staging package/retirement functions).
Require actual complete9-entry shell graph, both admitted landing IDs and all
production release/publication/static hashes. Bind only the matching immutable
revision plus current.json; never enumerate another repository or trust all
dist files. Copy shell9, regenerated production worker, content9 (including
admission/Website publication), two actual article pages plus sitemap/robots/
static publication manifest, and explicit Apache files. No fixed two-ID code:
derive supported article list from validated release/static manifests.
Exclude .vite, .wrn-stage-c-publication-staging and wrn-local-release from public;
retain Vite manifest solely in owned scratch to rebuild closed shell. Source
index is unchanged; public index gains meta CSP/referrer before shell hashing.
Article landing bytes/static manifest stay exactly unchanged.

Default production worker drops HTTP-only response headers on offline responses.
Therefore embed the same enforcing CSP in index meta (omit frame-ancestors,
unsupported in meta); HTTP header retains frame-ancestors none/X-Frame-Options.
No unsafe-inline/eval, remote media or frame provider permission. Existing
inline style/JSON-LD hashes must match admitted landing bytes. Rebuild worker
from modified index in scratch using ordinary production protocol/filename,
never staging namespace/origin. Copy only its final closed manifest paths.

Root Apache policy: explicit MIME, directory indexes off, CSP/COOP/CORP/
nosniff/referrer/permissions headers. No staging noindex or origin rewrite.
Hashed assets immutable; index/sitemap/robots/static manifest and worker no-store;
worker Service-Worker-Allowed /. Only wrn-production-content subtree permits
public credentialless CORS * and CORP cross-origin. There immutable revisions
cache immutable but current.json remains no-store; no redirect/fallback rewrite.
Use onsuccess unset plus always set where needed to avoid duplicate headers.
Header profile in external manifest describes per-resource expected values.
Reference checked primary docs (10.September):
https://httpd.apache.org/docs/2.4/mod/mod_headers.html
https://httpd.apache.org/docs/2.4/sections.html
https://developer.mozilla.org/en-US/docs/Web/HTTP/Reference/Headers/Content-Security-Policy
These support configuration design, not a claim of an executed Apache test.

External package-manifest binds sourceCommit, generatedAt, canonical origin,
source input hashes, current revision/sequence, shellID, exact public file paths,
byte counts/hashes/header profiles and activation order. New immutable files
first, current.json last; old assets must remain on actual server for rollback.
Manifest and operator readme live outside public. Include local verify function
which rejects tampered/missing/extra public files, mismatched source/shell/static
identity and unknown manifest fields. No uploading/promotion action implemented.

Tests: two byteidentical builds, actual reader IDs/content preserved, complete
shell/CSP/worker byte binding, no public test-fixture or scratch files, exact
header distinctions for pointer/revision/assets/worker/HTML, metadata mismatch,
tampered/missing/extra file and existing output preservation, real input/output
ancestor Junction rejection. Fresh retained test dirs only. Run scoped Node/
lint/format, provide completed handoff and actual package paths. Chief later
serves the final local package and tests browser CSP/offline/cross-origin fetch;
actual Apache/remote headers, rollout/rollback remain separately authorized
external gates. No signing/version/install/deploy or PO visual acceptance.
END-CHECK: :)
