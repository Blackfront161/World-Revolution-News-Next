# Bounded production media storage and transport seam inventory

Delegation: erlaubt. Root reserves Slot2 existing Terra/high after this gate,
read-only one bounded code pass, no children/browser/network/product/test/index
writes. Own only matching STORAGE-TRANSPORT-INVENTORY report/handoff. No A1/A2
review duplication; Sol owns independent A2. Root is integration owner.

Read accepted production media delivery design and actual new A1 types. Inspect
packages/browser-content/src/content-release-transport-core.ts,
production-content-release.ts, production-content-offline-store.ts and existing
apps/mobile/src/mobile-media-resume-store.ts plus their relevant tests. Identify
the smallest safe reuse for new shared media release transport, media safety
storage and resume factory, with separate client DB names. Do not change or
suggest relaxing existing fixed fixture types/paths or5V1pins.

Concrete questions: existing JSON transport returns parsed objects and fixes
the news path prefix, while A1 binds raw UTF8 bytes. Which helper boundaries can
be reused without losing raw byte identity,12s total/3s headers/BOM rejection,
abort responsiveness and cap-before-payload? Which existing storage operations
already provide CAS/generation/readback/deleteIfExact and protected future state?
What exact minimal new/adapted files, explicit negative tests and integration
order satisfy persist-safety-before-ordinary-payload, highestAcceptedSequence
across rollback/clear, client isolation and explicit-pause-only resume?

Return concise actionable seam map, reuse vs necessary new interface decisions
and risks; no architecture or release approval. Do not produce another broad
legacy inventory or hundreds of copied lines. Stop after useful bounded result;
no quota retry loops. Template handoff with END-CHECK: :) and allrights returned.
