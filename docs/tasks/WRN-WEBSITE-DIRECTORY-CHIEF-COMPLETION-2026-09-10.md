# Chief completion of preserved Website directory WIP

Parent Directory/full release brief. Writer ended with missing offline-reload
proof after repeated partial returns; all rights returned. Root takes the same
Website scope sequentially and Browser43173–75; Core is the sole other writer.
Existing WIP is preserved as a separate candidate, not technically accepted.

Root read reveals more than one missing proof: new graph accepts three files
of one JSON family, Vite duplicate references can substitute for a missing file,
per-file limits are absent, metadata merely checks count; original shell builder
tests remain unchanged despite new mandatory8-asset mode. Complete the bound
closed graph and meaningful tests, not just a happy-path offline screenshot.
Transport must cancel rejected bodies and recheck abort; graph asserts exact
unique family/path/MIME/hash/bytes bindings for new8 metadata. Original5-entry
metadata read compatibility stays; current builder requires3 JSON families.

Additional exact paths beyond original Directory brief:
tests/e2e/website-shell-built.spec.ts updates actualcurrent5->8 assertion with
explicit three approved JSON classes; old fixture article cache absence remains.
packages/ui-language/src/index.ts and existing eight catalogs, only
websiteShellScope: disclose cached library/help/source/news-directory metadata;
full article/media and preferences stay independently managed. This is needed
because existing copy incorrectly promises interface files only.

Keep existing builder five-asset fixture tests as explicit historical mode;
new builder default must require full8 graph. If a test-only optional legacy
mode would introduce a runtime bypass, use separate immutable historical
fixture-manifest reader checks and update existing builder fixture generator to
current8 assets, preserving its actual assertions and failure triggers.
Add distinct unknown JSON, duplicate family/Vite path, missing asset/orphan,
traversal/external/query, bytecap, wrongMIME/hash metadata rejections.

Use the real Website UI Save website shell, observe saved/active, reload under
network-off then visit Knowledge/Help/Directory from caches. Verify8payloads,
same-origin only, blocked invalid assets and clear user meaning. Add320px+
RU200/theme and desktop images; run affected suites/types/static plus actual
builder tests. Frozen Root candidate then fresh independent Terra QA required.
No worker control/lifecycle change, dependencies, Android or publication.

Concrete completion follow-on: the original directory acceptance also requires
source/language filters, pagination and malformed-loader browser rejection;
finish these in the already owned route/test. The 320px RU200 probe found
unbreakable headings and min-content list overflow; correct directory.css only.
The changed disclosure has one exact old-copy assertion in
packages/ui-language/src/index.test.ts; update only that approved sentence.
The dependent tests/e2e/website-shell-core-helper.mjs builds synthetic shell
generations and must declare the same three JSON families/MIME. Bind its builder
calls through a raw Node subprocess to preserve canonical function source bytes:
Playwright transforms imported MJS and breaks the immutable staging marker.
tests/e2e/website-shell-ui-lifecycle.spec.ts uses that same exported helper.
No test expectation or failure trigger is removed. Native worker source stays
untouched. These are necessary companion tests for the new current graph.
