# Production paragraph translation: implementation contract

Parent: original G2 additions and RELEASE-COMPLETION. Root's bounded reuse
inventory11September confirms existing SEC-001/R-24 on byte-pinned local legacy
Worker code. Feature remains incomplete; the current Home writer is disjoint.

Delegation: erlaubt. After this separate gatecommit Slot2 is reserved for one
fresh independent_architecture_reviewer Sol/high, read-only. No children, index,
browser, network/provider calls, existing secret/config reads or product writes.
Own only NEW docs/evidence/WRN-PRODUCTION-TRANSLATION-DESIGN-2026-09-11.md
and matching docs/handoffs file. Slot1 remains Home writer; Slot3 free.

Read the new translation reuse evidence/harness and original G2 requirements,
ADR005/008 and SEC-001 as needed. Do not repeat infrastructure inventory or run
an exhaustive scan. Bound question: what is the smallest target-owned,
versioned implementation that closes the server cache-key trust bug, reuses the
existing upstream translation service, and enables explicit paragraph translation
in both production readers without a competing reader or fixture imports?

Return exact proposed new/existing files, request/response/version identity,
canonical server cache key and namespace transition, bounded I/O, timeout,
original retention and stale-result cancellation, local mocked validation and
the concrete activation evidence still required. No arbitrary client key may
be authoritative; no client-only fix may claim to close SEC-001. Do not assume
that the local legacy file is today's deployment. Preserve separate client
storage and same black/red themes; new copy covers nine UI languages.

Reuse shared guards, existing deployment separation and upstream seam. Limit
network payload to the deliberately selected public paragraph and required
language fields; no tracking IDs, article URL, saved choices or automatic
provider fallback. Original remains visible on errors/offline. Do not silently
truncate text or accept response HTML. Reader identity must bind exact production
release, article revision, paragraph text hash, source/target and adapter version.
Original safety/expiry/revocation always dominates a translation.

Implementable local work must not wait on hypothetical approval. Conversely,
no live provider request/deployment or unverifiable cost/privacy claim is
authorized here. State the remaining concrete external evidence separately;
an inert disabled button is not completion. Any additional service needs only
the minimum existing-project configuration, no dependencies installation.

One bounded code/design pass, checkpoint, precise contract and direct handoff.
No generic architecture ladder or additional reviewers proposed without a
specific unresolved risk. Root will bind implementation ownership after Home
returns. Template handoff, rights return, WRN-AGENT-STATUS END-CHECK: :).
