# Production Events/Media directory integration

Parent RELEASE-COMPLETION, explicit PO continuation. Delegation: erlaubt, no
children. Independently GREEN data c80bd68 remains byte-identical. This slice
uses the real historical metadata and preserves the separate fixture players.
No provider playback, current event verification or redistribution claim is
inferred from importing an old link. No provider fetch before explicit consent.

## User result

Both clients show Events and Media directories instead of production fixture
content/placeholders. Events support search, original language, country/city,
date range and past/upcoming/all filters. Dates use an explicit displayed zone;
the date filter uses that same zone. Default all avoids hiding historical data.
Show source and supplied time/place, date of the historical source snapshot,
and a concise request to check schedule/cancellation at the source. Never label
an old availability flag as current. No location permission or map.

Media has Videos, Podcast episodes and Podcast sources, search/language/source
filters and original date/source metadata. Keep the eleven approved channel
links in the Videos view. Preserve uncertain/und language, visibly mark review
flags; do not attribute UI language to content. Source selection uses stable IDs
for episode joins, and repeated original observations are not duplicate cards.
No images, players, autoplay, embeds or audio downloads from the metadata packet.
Original HTTPS links require one accessible confirmation; HTTP remains plain
protected metadata. No invented HTTPS conversion. Card title language is the
recorded language, with und handled honestly. Render 30 at a time, show total,
more button; filters reset pagination and never truncate the underlying catalog.

## Writer and integration ownership

Slot1 existing production_mobile_b1 may own only these NEW source files after
explicit reservation: packages/browser-content/src/events-media-model.ts,
events-media-directory.tsx, events-media-directory.css; packages/ui-language/src/
events-media.ts and events-media.test.ts; apps/mobile/src/features/events-media/
production-events-media-ui.test.tsx. No existing file, app/route/build, generated
data, package, alias, shared reader, browser, index or child ownership.

Shared component accepts trusted load(signal), language, mode events/media,
headingId, headingRef, optional supplied heading level (Website H1/Mobile H2),
and optional videoChannels children. No storage/routing/app imports or singleton
client state. Shared model supplies filtering/sorting only; reuse strict public
production-events-media-v1 validation/URL/text rules. Load failures visibly allow
retry, abort/unmount or superseded load never commits stale state. Dialog traps
Tab/ShiftTab, Escape/Cancel returns exact connected trigger, body siblings inert
and restored on close. Confirmation URL clears on filter/mode/data identity
change; never activate stale selection. All nine UI languages use real copy,
shared current brand tokens and red outline/filled selection, >=44px controls,
320px and RU200 reflow, forced colors and keyboard semantics.

Root separately owns thin new client repository/route wrappers, App conditional
production routing (after Website Stage C QA closes), required public copy export,
exact alias/type path additions, narrow Website adapter tests and new browser
matrix. Root may add one shared bounded same-origin JSON loader if repeated
client code would otherwise duplicate safety rules; clients supply own emitted
URL, request lifetime and data cache. Existing fixture code/tests remain intact.

Website shell integration is a following explicit root sub-slice: extend the
closed asset graph from three to four known JSON families (8 to9 entries), keep
historical five/eight versions readable and bound all generations to the same
8MiB cap. No broad glob or raised threshold. Current generation4636284 bytes
plus metadata3317607 leaves434717 bytes before new code. Add exact generated
JSON paths to Prettier ignore; preserve SHA003c32358ebe0d5ad22fb9c0b059bf82c242aa0b99a4d869fb67960c86acdc07.

Root exact graph paths: apps/website/tools/build-offline-shell.mjs/.test.mjs,
apps/website/src/offline-shell/protocol.mjs/protocol-contract.test.mjs and
tests/e2e/website-shell-built.spec.ts. New9graph requires each original8family
plus exactly one production-events-media-v1 hashed JSON <=4MiB. Historical8
requires all original families and excludes the new family; old5read behavior
remains unchanged. Builder accepts original3or complete4families, rejects
duplicates/substitution/missing/orphaned assets, manifest/file/hash divergence,
each family cap and aggregate8MiB excess. Existing old graph tests retain8
assertions; actual compiled Website browser graph necessarily now has9because
the new real metadata URL is included even in the explicit fixture App bundle.
Its browser assertions therefore require9and exactly4namedJSONfamilies. Add new
positive9and malformed9 tests, not relaxed old tests. Root test-only fixture
helpers and copy labels are updated only where the graph scope is stated.

## Acceptance

Meaningful synthetic malformed/failed/retry/abort tests, actual metadata count,
search/date/country/source joins, uncertain language, pagination, HTTPS consent
and HTTP protection, nine-copy completeness. No arbitrary example fixture can
replace real-data acceptance. Root real browser both clients, 320/390/1440,
EN/DE/RU, RU200, violet/dark/contrast, Axe and exact focus; no provider requests
until confirmation. Website cold offline page with all four metadata families;
Mobile and Website independent builds and static checks. Preserve data/Reader
pins, publish source/image hashes, freeze candidate for independent Terra QA.
Sol only if an actual storage/protocol or safety contradiction requires review.

Own evidence/handoff WRN-PRODUCTION-EVENTS-MEDIA-UI-2026-09-10[-WRITER]. No install,
signing, version bump, publication, paid API or PO visual acceptance claim.
END-CHECK: :)
