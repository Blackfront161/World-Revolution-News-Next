# Independent checks of returned Website and Android candidates

Parent: `WRN-RELEASE-COMPLETION-2026-09-10.md`. Chief `/root` owns integration.
Delegation allowed to exactly the two direct reviewers below, no children,
index/commit/product/test changes. Core writer occupies Slot1 and temporarily
keeps existing package exports/indexes unchanged while these checks run.

## Slot2: existing Sol production_content_design, native integrity review

Candidate `b2b223e`, exact eleven changed files. Native writer is finished.
Read the bridge brief, code/tests and corrected signing-incident evidence.
Review strict Java/TypeScript URL/route agreement, cold/warm retained delivery,
back readiness and acknowledgement lifetime, cleanup/reload, app history/draft
integration, plugin/UI-thread state and text-only sharing. Distinguish actual
bugs from missing device evidence. Root suspects that history-session binding
and post-dispose event handling merit attention; these are questions, not findings.

Run bounded existing JS checks if useful. NO Gradle, SDK, browser, emulator,
device, build/signing/dependency action. Installed Capacitor/AndroidX source
may be read. Own only
`docs/evidence/WRN-NATIVE-PLATFORM-INDEPENDENT-QA-2026-09-10.md` and matching
handoff. Return findings with exact trigger, path/line, severity and smallest
correction; no open-ended architecture redesign. The observed execution breach
remains separate from product correctness. No device/release GREEN.

## Slot3: existing continuation_qa Terra/high, Website QA

Candidate `9a86e5a`, exact twenty changed files. Exclusive Browser43173–75;
keep preview43176. Own only
`docs/evidence/WRN-WEBSITE-KNOWLEDGE-SUPPORT-INDEPENDENT-QA-2026-09-10.md`,
matching handoff and a new uniquely named evidence subfolder for QA images.

Check the original implementation brief and its pin/static-graph clarification:
609books/22terms/11help/30historicalprofiles, nine UI languages, original language,
source links, search/filter, local draft persistence/discard/navigation guard,
no contact transfer. Verify actual accessibility, 320/390px RU200%, desktop,
three themes, screenshots/hashes, heading/focus/keyboard/44px/no overflow,
safe links and no unexpected third-party requests. Validate loader failure,
abort, valid-only cache and the two fixed JSON pins. Raw embedding and measured
1,099,139-byte JS are known; judge actual effect without silently changing the
closed offline graph. Run focused tests and relevant website regression/type/
boundary/build gates; no baseline assertion weakening.

Every Browser invocation must use a NEW unique output directory, never an
existing `test-results` folder. Save selected new images and hashmanifest.
No pnpm/install, cloud write, device action or product fix. If a definite finding
is reproduced, hand it back promptly with evidence. Avoid repeating passed
matrices without a changed file or unresolved concern.

Both reviewers bind start/end hashes for examined product and shared dependency
paths. A concurrent unrelated HEAD move is not candidate drift when relevant
bytes match. Hand back rights/browser and `END-CHECK: :)` when finished.
