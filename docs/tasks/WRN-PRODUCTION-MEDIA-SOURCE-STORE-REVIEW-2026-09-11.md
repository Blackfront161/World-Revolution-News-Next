# A4 independent source review and bounded A6 store design

Delegation: erlaubt. Root reserves Slot1 existing Sol/high after A3 closure.
No children/product/test/index/browser/network/native writes. Root handles A5
returned integration; Terra currently finishes its own A5 matrix. Own only
A4-INDEPENDENT and A6-STORE-DESIGN evidence/handoff plus own helper/output
directories. Two sequential related read-only questions, no wider architecture.

First inspect frozen A4 source/test/helper candidate9d4f884f under its A4 brief.
28focused and your distinguishing probes only: raw hashes, safety commit before
ordinary payload, exact/cumulative commit readback, total deadline across commit,
expiry, abort/late work, snapshots and genuine A1 ready. The trusted callback
alone is explicitly not disk evidence. Report narrow findings or GREEN.

Then specify smallest safe separate media release/safety IDB store using
unchanged createOfflineIdbCore. Must bind active/candidate/previous, generation+
clear CAS, exact durable safety callback for A4, highestAcceptedSequence surviving
rollback/clear,512 durable revision/sequence/hash identities with no eviction,
protected future/corrupt/over-cap DB and complete late/failed-write semantics.
Proposed bounds: control256KiB,3 bundles1MiB each,total3328KiB; raw wire already
bounded311296bytes but nested JSON serialization can expand. Reject any unsafe
bound/invariant and give exact minimal replacement, not a broad new platform.

Resolve explicitly: A1 validateRelease(knownSafety) rejects older revocation
revisions, while rollback requires retained old raw metadata. Explain how to
validate historical integrity separately from current availability without
inventing an A1 Ready or discarding cumulative revocations; define when older
bundle may be selected or must be unavailable, and floor/expiry semantics.
Clarify sequence idempotency vs replay: exact currently-known highest identity
may refresh; a lower sequence cannot re-enter after clear; rollback can only
select already retained safe previous without lowering durable high-water mark.
Current A1 safetySnapshot/merge are private; avoid copied domain validators.
Name any minimal additive public A1 helper(s) and regression tests needed, or
provide a way to use existing public operations without duplicating semantics.
State exact new store/profile/unit/browser file scope and discriminating tests.

Return concrete decision and both handoffs, including scope limitations. No
provider admission, real durable store or release GREEN implied by design.
