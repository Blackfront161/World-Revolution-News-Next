# Bounded continuation after runtime model limits

Parent RELEASE-COMPLETION, SITE-PACKAGE and DELIVERY-PACKAGE-CORRECTION.
Delegation: erlaubt. No children. Both previous Sol/Terra instances returned
usage-limit errors; do not retry or purchase/reset usage. Their evidence and
partial Site snapshot remain preserved. The completed delivery writer has
returned all rights. These three old instances hold no active reservations.

Root continues the bound UPDATE-SOURCE client scope and owns browser ports,
keeping the user's43176 intact. Slot1 is reserved for fresh site_package_finish,
inherited Root model as available fallback, exclusively the two new Site tool
files and matching SITE-PACKAGE-WRITER evidence/handoff. All other Site brief
requirements remain binding. Freeze only the validated public closure plus
Vite manifest into fresh owned input; preserve the earlier partial snapshot.
No existing source or build changes. Root and Slot1 are the only writers.

Slot2 is reserved for fresh delivery_correction_review, inherited Root model
as available independent fallback, read-only source review and tests of the
four findings plus Chief corrections at459b67e. Own only the matching
DELIVERY-PACKAGE-CORRECTION-INDEPENDENT evidence/handoff. No product/browser,
index, dependency or external actions. Slot3 stays free. Model fallback is
limited to these bounded tasks; no attempt to turn model limits into GREEN.

Each agent reads its existing brief and handoff, retains fresh test artifacts,
and reports evidence and remaining limits using the handoff template. Independent
source and final Site checks remain required after implementation. No install,
signing, version bump, publication, paid API or PO visual approval is implied.
END-CHECK: :)
