# Independent Website directory and offline catalogue QA

Parent full release completion; Delegation: erlaubt. Root reserves Slot3 for
one fresh qa_release_engineer Terra/high, no children. Candidate818612d and
base c297644 are frozen. Root returned Website product/test/browser rights.
Exclusive Browser43173–75, preserve43176. Own only evidence/handoff/probes/
new hash-bound images WRN-WEBSITE-DIRECTORY-INDEPENDENT-QA-2026-09-10.
No product/test/index/native/network/install/deploy writes.

Read the original Directory brief, Chief completion brief, current evidence
and actual diff, then independently judge this bounded slice. Confirm filters,
pagination,9UI languages,3sport notes/source links, known/unknown language and
source facts, loading/retry,HTTP endpoint containment and guarded navigation.
Reproduce117Website/8Copy,14Builder+2Protocol, relevant types/static and the
three Website test files (Directory, built shell, shell UI lifecycle) with fresh
unique output. Existing native core-matrix33PASS may be inspected; rerun only
if a concrete concern justifies it. Check the precise8-asset family/Vite/MIME/
hash/size/path binding and historical5 reader compatibility; probe a missing
negative case if found. Confirm user Save shell → network-off fresh reload
serves all3JSON families with eight cached paths and no foreign requests.

Visually inspect actual320/RU200 and desktop; retained32PNGmanifest must match.
Graph fixes and new filter/sport content require independent review, not merely
acceptance of the writer's test count. Confirm child-specific fonts/colors and
selected red buttons under violet/red,red/cyan/light. Do not infer PO acceptance.
CanonicalJSON snapshot intentionally compact; do not format it or report that
as a product defect. Test-only canonical Node builder preserves source hashes.

Slot1 B1 writer may create disjoint new Mobile files during QA but must not
change existing wrappers/shared index/Core until this QA returns. Candidate
binding covers all Website and ui-language files, not an unrelated moving HEAD.
Return concrete findings early and a brief independent verdict with exact
commands, source/candidate equality and rights handback. No broad re-audit.
