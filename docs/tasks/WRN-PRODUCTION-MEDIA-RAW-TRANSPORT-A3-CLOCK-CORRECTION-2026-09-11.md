# A3 clock M-001 correction

Root accepts independent A3-INDEPENDENT-M-001 after all reviewer rights return.
Exactly existing A3 reader/test plus matching correction report/handoff. Root
alone writes; A2 is frozen, Terra owns disjoint A5. No children or core/A1 edits.
Normalize throwing/nonfinite clock reads. Before session creation, throw only
fixed timeout; within session every such read must fail(timeout), abort it and
preserve that exact terminal for later calls. Cover begin/check/header deadline
and final boundary reads; clock reentrant close must not allow a request.
Add distinguishing RED regressions before fix;62prior tests retained. Run
focused/static/types and same-reviewer narrow closure after A2. No activation.
