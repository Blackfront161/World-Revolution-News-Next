# Root completion of the returned pure media contract

Standing completion mandate and A1/A1-COMPLETION contracts remain binding.
Terra returned its second incomplete checkpoint and all Slot2 rights. Root
sequentially owns exactly A1's package export, new contract and new test, plus
own matching ROOT report/handoff and status. No delegation/children, no client,
public content, provider, browser or legacy changes. Slot1 Sol independently
reviews only frozen image/reflow57b40252 and its artifacts.

Root inspection finds missing product clauses as well as test gaps: the pointer
has no descriptor byte binding; safety floor is not tied to descriptor; malformed
descriptor rows can pass phase1; same-revision safety identity conflicts are not
bound; scalar surrogate rejection also rejects valid Unicode pairs; noncanonical
URLs are accepted; empty/duplicate episode-stream coverage and required source/
episode metadata are incomplete. No cryptographic signature exists: say hash-bound
trusted metadata, never signed. This unconsumed WIP is not an accepted validator.

Complete the reviewed design through small pure helpers and exact readonly
types, shared structural preflight, detached/provenance-checked return values,
valid UTF-8 text semantics, all five document identities and needed metadata.
Public snapshot helpers may remain module-private. Add descriptorBytes to the
pointer. Canonical URL means URL.href equals input; HTTPS only, no credentials,
query, fragment, backslashes/control/encoded traversal. All record identities
remain opaque and unique, each episode has exactly one stream and no orphan.

Source admission includes validFrom/Until, owner/correction-contact reference,
separate localized source self/editorial descriptions, healthObservedAt and
healthy status. Episode metadata includes canonical page, publishedAt, title,
summary and duration, bound to the exact stream. Nine locale keys are mandatory;
source-language title present, unavailable translations null. Publication must
not postdate generation; admission, consent and rights windows cover the whole
release. Existing text maximum1000characters retained for localized values;
plain text and attribution are bounded, never rendered as markup.

Known safety records last revocation hash (null only for empty revision0) plus
revision/floor and cumulative typed target identities. Same revision accepts
only the identical revocation hash; higher revision preserves old blocked IDs.
Descriptor floor must match incoming revocation floor and never decrease.
Bound the cumulative ledger by the already approved128KiB document limit; reject
over-cap future input without discarding it. Phase1 validates pointer/descriptor/
revocation only, never reads ordinary document content or grants readiness.
Document byte bindings and total cap are checked before any hash await.

Tests must use correctly rehashed otherwise valid releases so each failure
distinguishes its intended rule. Exact UTF-8 padding covers pointer/descriptor/
each doc and aggregate cap-1/cap/cap+1; graph generation covers7/8/9sources,
7/8/9series,31/32/33episodes/streams. Audio byte/duration/validity and Unicode,
URL, identity, rights/consent windows, hash/mutation/safety conflicts get actual
positive/negative oracles. Complete focused and existing suites, Type/lint/format
and five old pins. Then freeze and request independent Sol contract review.
No UI evidence needed for this unconsumed pure module. Rollback is the isolated
new contract/export; no durable storage or external effects exist.
