# Production media controller implementation

Delegation: erlaubt, but this is not a start until Root records combined A6
independent GREEN and commits this writer gate. Existing Terra media_durable_writer
then owns exactly the eight NEW paths in CONTROLLER-AMENDMENT-2026-09-12 plus
own docs/evidence/WRN-PRODUCTION-MEDIA-CONTROLLER-IMPLEMENTATION-2026-09-12 and
matching handoff. No children/index/dependencies/A1-A6 changes/aliases/App/routes/
visible UI/provider/native. You are not alone; preserve every other edit. Browser
43173-75 transfers only after A6 independent reviewer returns it. User43192/93
and older43190/91 remain untouched. Root performs disjoint preparation only.

Authority: original design with ALL superseding94e4b4d1 amendments and all six
independent finding oracles. Use one controller composing existing A4/A6 and
A2/A5. Source disabled/empty origins by default. A6 Ready object identity must
remain unchanged through controller state, observers and A2 context. Exact
interfaces, no duplicate validators/transport/stores/players. The public factory
also exposes getState and cancelConsent forwarding the existing A2 cancellation
so a dismissed prompt loses authority; this adds no new playback semantics.

Resume duration is explicitly the current A1 stream's declared durationMs,
matched with exact episode/stream/release/streamRevision. Do NOT persist A2's
possibly slightly shorter observed playback duration as the identity duration:
A2 deliberately permits up to5000ms observed metadata tolerance. Snapshot the
declared stream duration when the explicit-pause callback owns that exact
current context; A2 supplies a bounded integer position. Add a distinguishing
case with allowed observed duration below declared, then reopen and offer the
same exact paused position using the unchanged A1 identity. Outside the allowed
A2 tolerance, A2 stops and no usable resume is saved.

First complete meaningful controller-core tests with explicit success and
negative invariants, then thin wrapper tests and actual blank-IDB seams. The
Mobile standard test graph must collect the shared core matrix. Cover every
original15group and amended deadline/safety/readback/CAS/no-op/cleanup/observer
case, including the new duration/cancel distinctions. Use injected fake Audio
but real A2, real A4 metadata transport and real A5/A6 IndexedDB in seam cases.
No request to fixture/provider audio. Do not weaken assertions to pass.

Run focused controller tests, full affected Mobile and Website suites once when
done, both types, scopes/static/aliases/boundaries and full new actualChrome
matrix with fresh outputs. Reuse unchanged A1-A6 evidence; no gratuitous broad
contract/native rebuild. Any unreachable test import or path outside these eight
must be raised before editing it. Root freezes code and arranges independent
QA/correctness review before visible UI consumption.

Return a complete working eight-path candidate and concise evidence (counts,
actual command/run paths, distinguishing cases, residual limits), not an
unimplemented design or a task announcement. No whole-app/release GREEN.
