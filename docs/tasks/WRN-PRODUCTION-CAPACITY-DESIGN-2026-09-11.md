# Versioned production capacity beyond the three-article pilot

Parent: RELEASE-COMPLETION, original Home1+5+further/news-supply requirements.
Root inventory: WRN-NEWS-BACKEND-REUSE-2026-09-11. Existing first-pilot cap3 is
explicit, not an accidental bug; the new Home UI cannot remove it. This task
prepares its smallest safe versioned continuation, not a new product scope.

Delegation: erlaubt, read-only Sol architecture review only after translation
design/addendum returns and Root separately reserves the slot. This file alone
starts no agent. No children, browser, index, provider or product writes. Own
only new CAPACITY-DESIGN evidence/handoff dated2026-09-11. Reuse the pinned
news counts instead of downloading27MB again or repeating source admission.

Question: specify a bounded production format/validation policy capable of
at least one lead, five main items and further news while preserving old V1/V2
acceptance/rejection exactly. Prefer the smallest additive version and shared
validation extraction over duplicate validators or a premature pagination
platform. Assess a bounded batch (for example up to64articles) within the
existing4MiB total envelope; justify exact article/resource caps with bytes,
image representation and existing store/parser behavior. No global cap increase
to old validators. No new reader semantics or unchecked raw27MBclient fetch.

Read content-release-core-v1, production-content-release-v1/v2/compatible,
production-content-offline-v1/compatible, shared transport/store/controller and
the existing production builder/delivery/site tools. Return exact minimal files,
new version identity, shared-core factoring, pre-payload safety validation,
post-acceptance resolver proof, safe old-client failure, saved/direct history,
rollback/Clear/sequence protection and discriminating tests. Keep actual two
EFF release packets and all fixture pins immutable; authored6/8/64article
protocol tests are test data, not claims about admitted real news.

Separate scalable mechanics from real source admission/ingestion. Reuse the
current static delivery chain and existing legacy news producer. Identify the
smallest subsequent deterministic ingestion boundary from pinned legacy input
to a candidate; articleComplete and imageURL flags alone grant no rights and
unknown language must not be guessed. No extra cloud service for a static
publisher unless concrete evidence requires it.

One focused implementable contract and test matrix, no general re-audit or
new review ladder. Preserve existing safety-first persistence, immutable
identities, monotonic highestAcceptedSequence across rollback/Clear, operation
epochs, bounded offline data and separate clients. No installation/signing/
publication; Root binds any product ownership after reviewing this result.
Template handoff with rights return and END-CHECK: :).
