# Complete the agreed compact Home images

PO standing completion mandate plus original G2 roadmap: five compact Home
stories with images. Current shared production-content-ui.tsx:504 only resolves
images for role lead. Three admitted original images in the five main stories
are therefore unnecessarily absent. This is a concrete remaining parity gap.

Delegation: erlaubt. Slot2 existing production_capacity_core_writer Terra/high,
no children, no browser/index/dependency/public/artifact writes. Root owns
integration and browser evidence; Sol Slot1 has disjoint media design only.
Writer is not alone: preserve all other changes. Read current source/truth and
quality rules. Own exactly:

1. packages/browser-content/src/production-content-ui.tsx
2. packages/browser-content/src/production-content-ui.css
3. apps/mobile/src/production-home.test.tsx
4. tests/e2e/production-home.spec.ts
5. matching WRN-HOME-COMPACT-IMAGES-2026-09-11 evidence report/handoff only.

Resolve one already admitted image for lead AND main roles, using the existing
ProductionReaderBlocks component and licence dialog guard. No external image
requests, copied data, second decoder, new caption/language policy or fallback
stock imagery. Further/discover/saved remain outside this Home presentation.
Main image fits a compact max-width around12rem while preserving intrinsic ratio,
full attribution/license action and reflow; no cropped attribution/alt text.
No empty image placeholder for the two articles with no admitted image.

Add a meaningful main-role regression with admission/safety/hidden-source
behavior, not just checking source text. Existing hero negative assertions stay.
Adapt existing13 Home E2Es to four total images (one lead/three main) in the real
six-article input, exact stable IDs, no pictures for the other two, license
dialog/focus for a main image, hidden source removes all EFF pictures, offline
Home retains main images. Preserve320/390RU200/1200 and both themes. Keep all
other test semantics; no fixture edits or expectation weakening.

Writer runs focused Mobile home Vitest and scoped lint/format/type checks using
existing direct Node tools; no pnpm install. Root runs fresh existing13-browser
matrix and builds both clients after handoff, preserving screenshots/hashmanifest.
Independent review follows frozen candidate. Return short template handoff with
files, exact tests/failures, risks and END-CHECK: :). One bounded implementation
pass; if incomplete return WIP instead of starting repeated broad test loops.

Root artifact ownership: new WRN-HOME-COMPACT-IMAGES-ROOT-2026-09-11 directory,
report/handoff and status. Build both clients from the committed candidate and
unchanged public sequence3 data; preserve all earlier builds. Fresh package clock
is actual build start. Previously generated Website publication time remains its
true earlier value; require it to precede this build and match static publication.
Native staging/unsigned build follows the existing guarded offline init script,
no signing/install/version change. Root may use new preview43188/89 and must
update the visible current preview after accepted build. Independent review
binds exact candidate and artifacts, no new source-admission decision.
