# Requirements reconciliation and authorized Android test

- Parent: WRN-RELEASE-COMPLETION-2026-09-10; baseline084b992/product26c819f.
- PO explicitly approves the previously specified separate test APK signing and
  installation in existing WRN_API36_Play, then asks why images and earlier
  elaborated additions are missing. This supersedes the earlier test-sign/install
  waiting gate only. No production signing, version change, deployment or upload.
- Chief/central allocator/integrator: /root. Delegation: erlaubt, max2 direct
  read-only helpers; no children. Register: docs/WRN-G3-021-DELEGATION-REGISTER.md.
- Slot1 requirements_reconciliation: Luna, one bounded code/requirements pass;
  no quota retries. Slot2 existing delivery_correction_review: independent image
  contract integration design, inherited fallback after prior Sol quota failure.
- Root owns native script/evidence and state correction. Helpers own only their
  two REQUIREMENTS-RECONCILIATION and PRODUCTION-IMAGES-DESIGN evidence/handoff
  markdown files respectively. No concurrent product writers.

## Scope and evidence

Read current Charter, Source-of-Truth, architecture, quality, original G2 user
messages and later PO-092 decisions. Historical worktree is not a code source.
Compare promised features to reachable production routes, not fixture success.
Bind missing image support to actual contract/renderer/import and identify a
versioned reuse path that preserves existing IDs, safety and offline behavior.
Product changes follow a concrete file-bound supplement after this review.

Root may create docs/evidence/WRN-ANDROID-EMULATOR-2026-09-10/ scripts, evidence,
signed test-copy and a fresh ignored test-only keystore; no existing secrets
search/read/copy, no overwrite of unsigned input or existing user state. Root
may update this brief, PROJECT-STATE, AGENTS, readiness and delegation register.
Existing SDK/JDK tools only. Boot WRN_API36_Play without snapshot saving, inspect
package before installation; never uninstall, clear or downgrade an existing
app. If signing conflict exists preserve it and report the actual blocker.

## Acceptance

1. Honest production/fixture/missing matrix with concrete current code evidence.
2. Separate signed APK bound to immutable unsigned SHA25683943c42bc26d5540f436eb95df4947ad567ce657c0a28f542e360c6f41ea05b;
   test certificate and verification recorded without passwords/private key.
3. Existing emulator boot/install/start, native Back, lifecycle, saved reader
   restart/offline smoke and fresh screenshots where environment permits.
4. No production or Play-update success inferred from emulator smoke.
5. Subsequent image integration must test schema rejection, provenance, render,
   missing assets, offline/revocation and both clients; no remote hotlink default.

No dependencies/program installs, API costs, external writes, data deletion,
Git history changes or branch merge. Keep original artifacts and histories.
Rollback is choosing preserved prior artifacts, not overwriting old state.
Helpers deliver template handoffs with sources/files/tests/open risks and
WRN-AGENT-STATUS including END-CHECK: :). Token/cost measurement unknown.

## Native preservation supplement

Read-only emulator inspection finds existing WRN2.1.0/code25. Preserve it and
its signing identity. Root may stop only its own emulator-5556 instance and
restart WRN_API36_Play with a fresh explicit data/cache directory under ignored
local-qa/wrn-emulator-20260910/; the existing SDK factory data is the only
initialization source, never existing user data. No uninstall or wipe-data.
This is a clean-install smoke, not an upgrade verification. Keep all generated
test artifacts; native read-only session may be stopped after evidence capture.

## Image evidence supplement

Root may read EFF original pages/policy and obtain exactly their two main banner
images under docs/evidence/WRN-PRODUCTION-IMAGES-ADMISSION-2026-09-10/. Bind original
snapshot, placement, alt, explicit credits, MIME/dimensions/hash and policy.
User's image implementation request authorizes these local source candidates;
no automatic product admission of an unverified asset or broader website crawl.
