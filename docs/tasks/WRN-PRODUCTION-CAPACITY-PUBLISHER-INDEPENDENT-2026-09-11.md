# V3 publisher independent closure

Delegation: erlaubt. Root reserves Slot1 existing Sol
production_translation_independent, no children. Client c239aec4 independently
GREEN and all its rights returned. Review immutable publisher candidate d34c0cdc
against returned WIP49bf16e9 and accepted coreb828ce63, without new architecture
round. Own only new CAPACITY-PUBLISHER-INDEPENDENT evidence/handoff/output.
No product/tests/index/browser/network/provider/install/deploy writes.

Close receipt HIGH and descriptor MEDIUM from Root correction brief. Independently
verify supplied admission digests cannot be silently re-signed, invalid versions
reject before payload, old V1/V2 remain unchanged, V3 caps and exact bytes/count
refusals, 6/8/64 twice determinism, delivery pointer-last and actual six-landing
site package. Root final35NodePASS plus real result in correction evidence.
8MiB is shell aggregate/per-file package cap; content envelope is separately4MiB.
Sequence1 derivative is test genesis only; reviewed real input sequence3 is fixed.

Root holds publisher/tool paths frozen. Concurrent Terra transport writes only
the separately bound shared transport/tests and one additive readonly V3 cap
export, no validator semantics. Review pinned publisher and existing core logic;
note that single additive export when comparing worktree hashes. Root may stage
new local preview artifacts but will not activate client/public before GREEN.
Use existing binaries; no install/validation bypass. Focused relevant reproduction
and own distinguishing negative oracle, report findings directly to Root and
return Slot1 with END-CHECK: :).
