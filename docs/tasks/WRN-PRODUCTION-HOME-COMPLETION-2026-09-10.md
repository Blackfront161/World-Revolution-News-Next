# Complete the actual production Home composition

Parent: original G2 requirements, PO092 and RELEASE-COMPLETION. The PO asks
to finish previously agreed additions. Source UI e77767d3 is frozen under
independent recheck; this begins only disjoint read-only seam inventory.

Stage1 Delegation: erlaubt. Root reserves Slot2 fresh Luna home_production_inventory,
no children/product/test/index/browser/native/network rights. Own NEW evidence
WRN-PRODUCTION-HOME-INVENTORY-2026-09-10.md and matching handoff only.
Slot1 source QA stays independent; Root prepares disjoint preview/status.

Bound question: identify the smallest real-client integration for the accepted
Home structure (one lead, up to5 compact main items, sport, then further news).
Inspect the current production content area and existing G3-016/domain Home
rules, exact production article fields, available directory sport records and
safe V2 image projection/ownership. Do not re-audit unrelated features or old
repositories. Earlier Root inspection establishes actual Home currently uses
generic ProductionContentArea in both clients, plus MobileHomeDirectory only
on Mobile; use this fact instead of repeating the whole architecture inventory.

Map reusable public functions/styles/copy and precise proposed files/tests,
with particular care that optional hero images must respect the same verified
bytes, lifecycle, safety and blob cleanup as the reader. Do not suggest bypassing
reader guards, remote hotlinks, fixture imports, fake article counts, fabricated
current sport or a new independent reader. Prefer a small modular projection
and existing presentation seam. Identify which part is UI-ready versus blocked
by actual content supply: the admitted bundle contains only2EFF articles and
one image; directory sport has3 attributed reading notes, not a live sports feed.

A source is chosen only by existing stable IDs/explicit metadata; source
follow/hide must apply consistently without losing safety, saved/direct access
or original language. Existing black violet/red and optional red/cyan remain.
No Globale Lage, map, game, accounts, comments or external publication scope.

One bounded code pass and concise evidence. Recommendations are not an
architecture/release GREEN. Root binds exact product scope only after the
inventory and source QA return; original contract/native/content bytes frozen.
Handoff per template with commands/read paths, unknown costs, limitations,
rights return and WRN-AGENT-STATUS END-CHECK: :).

## Stage2 Root disposition and bounded implementation

Source e77767d3 independently GREEN; all reviewer rights returned. Luna returned
its two reports. Root accepts the identified production/image seams but does
not introduce serialized editorial roles merely to lay out an already sorted
list. This first production Home uses the existing validated chronological
list after source choices: first available article is lead, next up to5 are
compact main items, the remainder is further news. This is an explicit local
presentation policy, not an editorial curation or producer-data claim. A future
editor-assigned priority would require its own versioned source contract.
Production V2 descriptors/manifests, admission bytes and all native code stay
unchanged. Source checks/expiry/safety remain the existing UI authority.

Delegation: erlaubt. After the separate gatecommit Root reserves Slot1 exactly
one fresh frontend_brand_engineer Terra/high writer, no children. Sole product
writer; Root works only disjoint evidence/planning. Writer owns these existing
paths only:
- packages/browser-content/src/production-content-ui.tsx
- packages/browser-content/src/production-content-ui.css
- apps/mobile/src/App.tsx
- apps/website/src/App.tsx
- packages/ui-language/package.json
- tools/browser-content-aliases.mjs
- tsconfig.base.json
New owned paths:
- packages/browser-content/src/production-home.tsx
- packages/browser-content/src/production-home-selection.ts
- packages/ui-language/src/production-home.ts and production-home.test.ts
- apps/mobile/src/production-home.test.tsx
- tests/e2e/production-home.spec.ts
Own WRN-PRODUCTION-HOME-WRITER-2026-09-11 evidence/handoff and PNG directory.
No other old files may change, including source preferences, MobileHomeDirectory,
its selector/tests, reader image renderer, contracts, core stores, data/assets,
fixtures, dependencies, production publication, native code or signing files.

Implementation:
1. Extract the existing production card markup into an internal render helper
   in the same UI file, keeping every normal/saved/archived action and behavior.
   Production Home only uses shared ProductionHome presentation with that helper.
   Lead and main cards retain source/profile/save/read controls. Compact main
   cards use tighter layout and at most their existing teaser, not raw HTML.
2. New pure selection module takes already validated/filtered article arrays:
   one optional lead,0..5main,remaining; no mutation/duplicates. Do not reinterpret
   source data, news tags as sports labels, or import local fixture contracts.
3. New shared Home component receives an optional client-owned directory loader
   and navigation callback. It loads the validated projection ONCE per lifecycle,
   with failure/retry/abort/stale-result handling. Mobile adapter invokes existing
   loadMobileContentDirectory; Website adapter uses loadWebsiteContentDirectory
   with the supplied signal. Pass these through optional ProductionContentArea
   props; direct old test callers remain supported. Do not copy cache/SW logic.
4. Order: Featured lead, Latest main items, clearly labelled Sport reading notes,
   further production stories if present, then up to5 archive metadata links.
   Sports use only existing three validated sport notes and existing localized
   sport labels/attribution/original links, including their explicit non-live
   disclaimer. Archive links keep snapshot date and original-language/source
   metadata. Source preference projection uses exact directory endpointIds,
   after stable language/date/ID ordering and before archive cap. Reuse the
   already bound algorithm semantically but do not import an App module into
   shared/Website code. Single source of truth for new Home projection is the
   new shared module; older standalone MobileHomeDirectory stays for fixture
   compatibility and its frozen tests. Actual production Mobile branch removes
   only its redundant old HomeDirectory child, retaining fixture branch.
5. Both clients wire the same Home sections; all nine interface languages use
   complete copy. Use existing UI keys when accurate; new archive/selection/
   further-story labels belong in new ui-language subpath with its package,
   root TypeScript and browser aliases (only those additive entries).
6. Optional lead image comes ONLY from resolveProductionArticleView(visible,
   lead.id) and its existing validated V2 image block. Render it through existing
   ProductionReaderBlocks with only that block, so decoding/blob cleanup/alt/
   dimensions/attribution stay identical. No new renderer, remote URL or store.
   Hide/expiry/revocation/route guard loss unmounts that image immediately.
7. Evidence links: extend the existing openExternal helper only to accept an
   explicit article ID for a Home image (default remains routeArticle). After
   the existing fresh guard, resolve that exact article and permit only its
   original URL, text license URL or an admitted image block's license URL.
   This also closes the existing silent no-op for a valid image license that
   differs from its article's license. Preserve identity/route/expiry rechecks,
   external dialog/URL hygiene and focus restoration; do not accept sourceUrl
   or arbitrary URLs. No change to sharing or general URL policy.

Acceptance: real V2 Home shows first EFF image and2actual articles, no fake
6-card population or sports feed; both clients show real3sport notes and up to5
archive links. Pure tests cover0/1/2/6/8items, uniqueness/order, source hide/follow
and refill before cap. DOM tests cover image only from admitted block, mixed
article/image license whitelist, failed/stale directory load and old card action
parity. Actual browser matrix: Mobile/Website,320/390/1200px, black violet/red and
red/cyan, RU200%, keyboard/forced colors/Axe, source hiding including newHome,
reader/save return, licensed evidence link and full Website offline restart.
Explicitly use --project=mobile-390x844 with absolute production origins43177/78;
no unqualified seven-project invocation. Fresh output and retained manifest.
Run focused tests, Mobile/Website types, scoped static/boundaries and both builds;
Root owns full regression/independent candidate checks after writer return.
Do not modify old assertions to match a regression; report actual incompatible
test/semantic boundary first. No global styling, native, contents or dependency
changes. Stop with handoff when bounded candidate is testable; no Git index use.
# Root continuation after writer return, 11 September

Writer returned all rights at metadata38f9553c. Root reproduced601Mobile,
127Website,21UiLanguage,61WebsiteNode,three typechecks and19source/update
browser cases. Direct RU200% PNG inspection found sport metadata split into
near-single-character lines despite no horizontal overflow. Root owns only the
same CSS and new Home browser test to stack metadata at narrow widths and add
a real date-line-count oracle. Ensure lazy hero bytes load before RU captures.
No new feature, data, contract or reader-renderer scope. Retain original PNGs;
new Home13browser run and the already-bound independent QA verify the fix.
