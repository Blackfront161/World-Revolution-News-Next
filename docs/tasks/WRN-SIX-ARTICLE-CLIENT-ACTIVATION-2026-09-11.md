# Six admitted articles in both local clients

Main: Root only. Delegation: erlaubt only for independent final QA after this
candidate; no new agent instance or children. Standing PO completion mandate.
Publisher d34c0cdc and capacity transport each require their bound local and
independent GREEN before client/public data activation. Root may now prepare
own new SIX-ARTICLE-CLIENT-ACTIVATION helper/harness/report/output files only.
Other writers keep their exact transport and review scopes.

After both closures, Root owns generated new revision directories under
apps/mobile/public/wrn-production-content and apps/website/public/wrn-production-content,
and only their existing current.json pointers. Generate from inputd61e33e1 with
unchanged sequence3 and safety1 using the verified builder/static publication.
Retain all older files/pointers in Git and every existing preview. No manual
editing of generated records, article receipts or manifests.

Root also owns package.json only to add the existing V3 builder test file to
the standard test command: current explicit list omits it. No dependency or
lockfile mutation. Installed pnpm workspace mismatch remains a separate local
reproducibility issue; no install or verifyDepsBeforeRun bypass here.

Build both clients into fresh own directories, integrate all six website
landings and unchanged shell limits, verify exact manifests and default-disabled
translation. Prepare immutable native webassets/unsigned APK only through the
already authorized native build workflow; no signing/installation/version bump.
No remote/hosting/provider/signing action. Keep old previews43176/79/80.

Root browser ports43183/84 for fresh built Mobile/Website preview;43185/86 only
if needed for isolated Vite-based actual IDB continuity tests. No external requests.
Verify six original articles/four images,1+5 Home, attribution/dialogs, exact
image bytes, original language, routes and separate database profiles, saved/
offline reopen, black violet-red/cyan-red themes,320/390RU200/1200 and Axe/keyboard.
Current6 supply has no further article beyond1+5; do not invent seven/eight.
Both old V1/V2 and mixed-version store continuity remain in transport acceptance.
Save fresh selected screenshots and hashmanifest outside temporary output.

Deliver usable new preview in Codex and an inline evidence image. State that
the old existing-origin database is intentionally not cleared/rebootstrapped;
installed updates use the separately verified update chain. No PO visual
acceptance, continuously live ingestion, provider activation or release GREEN.
Update PROJECT-STATE, requirement status and handoff with actual artifact pins.

## Acceptance alignment after real activation

The generated input d61e33e1 is now active locally with exactly six articles,
five EFF and one C4SS. Root may additionally update only these existing E2E files:
tests/e2e/production-home.spec.ts, source-preferences.spec.ts,
website-personalization.spec.ts, production-content-ui.spec.ts and
production-website-content.spec.ts. Their default-production assertions still
expect the old two-EFF pilot. Explicit authored packet tests retain their own
unchanged counts and safety semantics. Record the old default-count RED before
changing expectations. Keep exact identity-based saved/direct-article checks;
after hiding EFF require the remaining C4SS identity, not an empty directory.
The translation wrapper requires paragraph selectors to target the original
paragraph element; keep the original twelve paragraphs and content oracles.
Pin the positive public test clock inside the release's validity interval.
No product changes, fixture swapping, skipped assertions or weakened CSP.
Run all five affected specs on the actual current production entries, with
fresh browser outputs. Independent final QA includes this test-only alignment.
