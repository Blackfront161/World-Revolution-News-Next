# Complete the incomplete pure media contract

Continuation of A1, WIPc1c6f72c. Delegation: erlaubt. After this gate Root reserves
Slot2 existing Terra/high, no children, exactly the same three product/test
paths plus own A1 evidence/handoff. Root image/reflow artifact work and Slot1
independent review are disjoint. No browser/index/client/provider rights.

The preceding handoff is not close to acceptance: rights/admission/consent are
only checked as arrays; graph identities and object keys are incomplete;
Object.freeze affects only the outer wrapper; ready has no module-owned proof;
new revocations do not prevent ready; source/series/stream revocations are not
applied; mutable clock/origins/safety can cross hash awaits. These are concrete
missing product safeguards, not merely missing tests. The current contract has
no consumer, so no playback can be enabled from this WIP.

Finish the full original A1 brief and accepted design within its existing paths.
First author a real passing synthetic one-episode input generator in the test
file and effective negative cases for these missing guards. Implement complete
exact-key types/validators for all five documents, all rights-basis variants and
coverage, graph binding, fingerprinted streamRevision, cumulative safety and
deep detached branded readiness. Expose the separate validated safety phase
needed by persist-before-payload transport; it must not mint a ready object.
Snapshot mutable injected inputs before every async boundary and validate
known safety structurally. Hash raw text only after rejecting invalid surrogate
sequences/UTF-8/BOM; canonical identity hashing is separate from wire hashing.

Do not stop after two negative tests or typecheck. Complete the specified valid,
invalid, mutation, identity and relevant cap boundary matrix, then run focused
and existing contract suites plus type/lint/format once. If a genuine specification
ambiguity prevents correctness, identify that exact question with minimal example
before inventing policy. A large but specified task is not itself a blocker.
No unrelated refactor. Keep five legacy pins exact. Return full result/remaining
defects honestly using A1 handoff; Root will reproduce and Sol review separately.
