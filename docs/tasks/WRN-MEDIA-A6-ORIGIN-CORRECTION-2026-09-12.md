# A6 current-origin correction and independent closure

Delegation: erlaubt. Root accepts the independent finding A6-IND-M-001 on
accumulated candidate1f62abdd. Regional client375574b5 is independently GREEN;
all prior review rights and ports have returned. After this separate gate commit
existing Sol writer media_durability_completion owns exactly these four paths:

- packages/content-contracts/src/production-media-v1.ts
- packages/content-contracts/tests/production-media-v1.test.ts
- tests/e2e/production-media-offline-harness.ts (only if the public probe needs it)
- tests/e2e/production-media-offline-store.spec.ts

Plus own docs/evidence/WRN-MEDIA-A6-ORIGIN-CORRECTION-2026-09-12 directory and
matching docs/handoffs report. No store/schema/provider/UI/alias/native/index
changes. You are not alone; retain all other edits. No children. Exclusive
blank harness ports43173-75 until returned; Root uses accepted43192-93 only.

Use one shared detached validator for both ordinary A1 and historical current
activation: exactly1..8 distinct canonical HTTPS origins under the existing
safeUrl rule. Reject malformed or oversized input before minting Ready. Preserve
ordinary A1 semantics and historical safety/hash/provenance behavior. Invalid
runtime inputs must have finite failure, not an uncaught iterator/type exception.

Add distinguishing helper and actual public A6 activateCandidate/readActive/
rollback tests covering0/8/9/malformed origins; eight valid origins including
the stream origin must succeed, absent stream origin must fail, rejected calls
must not damage the stored state. Snapshot caller input before any await and
retain a mutation-after-call oracle. First reproduce the distinguishing defect
against the prior source, preserve the output, then correct it. Run both focused
A1/A6 contract suites, complete bound A6 Chrome matrix, affected types and scoped
static/alias/boundary checks. Full contract run only when Root releases CPU load.

Return exact evidence/counts and all rights. Root freezes the candidate and binds
the same independent reviewer media_resume_independent to close only this finding
and necessary regressions; no self-review or new architecture round. Real provider,
audio, consent UI, native and overall release remain outside this correction.
