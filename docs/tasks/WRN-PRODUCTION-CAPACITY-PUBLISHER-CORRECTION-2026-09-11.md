# Publisher — Root correction and completion after returned WIP

Existing publisher brief remains binding. Writer returned incomplete; Root
preserves WIP in preceding commit and assumes only its same eight allowed
publisher/tool/test paths after this separate gate. No delegation for writing;
Sol concurrently reviews frozen client c239aec4 with43181/82, no overlapping
Root browser/client writes. No core, data input, client public, provider, deps
or deployment mutation. Own new PUBLISHER-CORRECTION evidence/handoff/output.

Root findings:
1. HIGH: V3 builder unconditionally recomputes admission digests. It silently
   accepts modified article/detail prose with stale approved receipts. Builder
   normalization adds outer revisions only, so no such re-signing is justified.
   Add original receipt tamper RED oracle, remove recomputation, retain strict
   supplied receipts and prove unchanged actual6 and authored8/64 still pass.
2. MEDIUM: static loader treats every descriptor that is not validV3 as legacy;
   unknown/invalid versions read payload before failing full factory. Add the
   compatible descriptor guard before payload and an unknown-version oracle
   with valid pointer hash and deliberately absent remaining resources.
3. Incomplete acceptance: no V3 site package, declared/actual reader cap
   refusal, or8/64twice determinism evidence. Complete these original criteria,
   keeping8MiBpackage/V1V2caps fixed. Root may create a V3website build in a
   fresh own directory via existing APIs, substituting only staged production
   content before final immutable package. Never overwrite existing previews.

Tests must retain actual release sequence3 in real candidate; genesis derivative
is test-only and cannot prove a production update chain. Existing ledger-chain
tests remain unchanged. If actual6package exceeds8MiB, record concrete graph
bytes/cause and bind the smallest separate packaging correction; do not raise
limits or silently omit promised sections. Final candidate+independent review
before shared transport/client activation. No extra architecture review round.
