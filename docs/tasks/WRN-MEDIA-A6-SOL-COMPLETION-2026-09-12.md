# A6 writer rotation and bounded completion

Root preserves the returned Terra checkpoint in f03fe37a (two actual Chrome
cases). After three incomplete bounded turns Terra is ended, all rights returned.
Slot2 now belongs to one fresh Sol/high worker for the remaining A6 durability
implementation and matrix. This model escalation is limited to cross-layer
storage/hash/transaction correctness. No children or further delegation.

The exact fourteen product/test paths and design in
WRN-PRODUCTION-MEDIA-A6-IMPLEMENTATION-2026-09-11.md remain binding, together with
every case in WRN-MEDIA-A6-DURABILITY-COMPLETION-2026-09-12.md. Do not expand
architecture, alter A2-A5, old V1, UI, aliases, dependencies or native code.
Own report/handoff/helpers/config/output go under WRN-MEDIA-A6-SOL-COMPLETION-
2026-09-12. Browser43173-75 exclusively belongs to this writer. No index/commit
writes. Root owns disjoint regional selection/UI; you are not alone and must
preserve other edits. User43190/91 and every previous evidence artifact stay.

Inspect and fix the remaining concrete defects before declaring completion:
historical integrity/identity/orphan verification before any mutation;
snapshot every caller object before awaits; close/versionchange abort tracked
transactions; attach and observe transaction completion immediately; precommit
and postcommit exact readback; correct 512-cap failure; same-highest release
refresh/activation semantics. Preserve initial control in versionchange and
the existing bounded getAll, strict database names and deep freeze fixes.

Return only after the complete required populated IDB/corruption/race/cap matrix,
focused A1/offline contracts, both client types and scoped static gates have run,
or a concrete external blocker makes further progress impossible. Do not end
with a promise to continue. Do not run the full contracts suite concurrently
with Root tests: coordinate first; narrow tests are allowed. Retain reproducible
blank browser runner and actual command outputs in owned evidence. Full product,
provider, UI and release gates remain open even if this A6 subtask passes.
