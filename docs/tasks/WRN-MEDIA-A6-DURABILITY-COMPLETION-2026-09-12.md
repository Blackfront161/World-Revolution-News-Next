# A6 required durability completion

Root preserves the returned A6 candidate separately. Its one empty-database
isolation test does not satisfy the binding A6 design matrix. No A6 completion
or acceptance is granted. Same Terra writer, Slot2, same exact A6 files and
exclusive43173-75, no children or shared alias writes; fresh own evidence/output.
Root separately completes regional contract and Sol can review that read-only.

Before another handoff, implement the required distinguishing actual-IDB matrix
in the existing A6 harness/spec, using local valid hash-bound A1 releases:

1. A/B/A activation and rollback; expiry/floor/current revocation blocks old A.
2. Durable exact safety readback before A4 payload; safety survives failed
   payload, restart, clear; no lowering safety/high-water/512 identities.
3. Exact highest refresh, same-sequence tuple conflict, lower replay after clear,
   511/512/513 no eviction and finite correct quota failure.
4. Hash/identity/bundle corruption, dangling/orphan slots, missing/extra control,
   extra stores/indexes, future version, all caps; raw state unchanged on reject.
5. Caller/expected/input mutation across hash awaits, late hash after clear,
   concurrent generation/clear CAS, abort/close/versionchange/quota/readback
   failure; no late write or readiness resurrection; reciprocal client isolation.

Root source inspection already finds concrete missing protections that those
tests must distinguish: factory accepts arbitrary database names; getAll is
unbounded; readState does not verify historical hashes/identity linkage before
clear or any write; absent control with empty bundles is reinitialized outside
version0; orphan bundles are accepted; nested returned control is not deeply
frozen; close/versionchange do not abort tracked transactions; incoming/expected/
input fields are reread after awaits; write lacks precommit readback. Correct
these within original A6 scope, not by weakening tests. Inspect same identity
refresh/activation semantics against the agreed source/controller flow too.

All are already bound design requirements, not a new architecture round.
Reuse A1 proof/safety validators and unchanged IDB core. Finite public errors,
exact snapshots before awaits, bounded reads, side-effect-free historical
validation before mutation, transaction-complete plus exact readback, protected
corrupt/future storage, and post-await CAS must hold. An intact expired bundle
is unavailable; corrupt bytes are incompatible and preserved. Do not treat a
failed historical proof as revocation and delete it.

Run full required unit/static and browser matrix with actual local Chrome,
retain reproducible config/server/helper/output in your own A6 evidence folder.
Return actual commands and counts, remaining failures and rights. No partial
one-case coverage as complete. Existing user preview and all old files stay.
