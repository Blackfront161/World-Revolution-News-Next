# Bounded production media first-boot and UI design

Delegation erlaubt, no children. After this separate gate existing Sol
media_resume_independent owns read-only design and exactly two own files:
docs/evidence/WRN-PRODUCTION-MEDIA-FIRST-BOOT-DESIGN-2026-09-12.md and matching
docs/handoffs/WRN-PRODUCTION-MEDIA-FIRST-BOOT-DESIGN-2026-09-12.md.
No code/tests/browser/index/provider/network/native writes or actions. A7 Terra
retains its eight paths/43173-75; the other Sol reviews frozen Root selection UI.
Root integrates and prepares source evidence. This is the permitted third
read-only slot, not another writer or agent pool.

Bounded question: recommend the smallest honest path from accepted A1-A6 plus
amended A7 public design to visible real episode/consent/resume in both clients,
including first startup on blank IDB. Do not audit the whole repository or the
unfinished A7 implementation. Read existing APIs/briefs and exact current
MobileProductionEventsMediaRoute/WebsiteProductionEventsMediaRoute plus shared
events-media-directory.tsx only as needed. Preserve original directory links.

Important observed constraint: A4 raw transport only accepts exact HTTPS
metadata origins. Current user previews are HTTP127.0.0.1; native Capacitor uses
https://localhost and future public website https://solinaridao.com. No new public
metadata has been deployed. A7 mount reads local only and initial source defaults
null/empty. A visible controller mounted on a blank store thus has no real episode
without an explicit initial content path. Do not claim mocked fixture playback
or unreachable disabled controls fulfill real product integration.

Consider a bounded bundled/local first-release input versus explicit same-origin
HTTPS metadata load, then recommend one minimal approach. Preserve A1 genuine
Ready and A6 durable safety-before-activation, current policy/time/origin checks,
no implicit Audio/provider contact before a current per-episode user gesture,
no fictional Response.url / URL remapping that pretends a remote fetch occurred,
no disabling transport/CSP/rights guards for a preview. No deployment, signing,
provider permission or user consent is inferred by this design.

Provider-policy exact-pair extension is independently approved as design only
(WRN-MEDIA-PROVIDER-POLICY-INDEPENDENT-2026-09-12). It must be implemented/reviewed
before admitting the observed Archive privacy relationship. Existing EFF evidence
is linked in Root delivery/range reports; no new probes are authorized here.

Specify concrete minimal files/APIs, startup/check/failure states, current
consent disclosure and cancellation, pause/resume/continue, route/unmount/focus
lifetime, original-link fallback, 9-language/shared UI placement, bounded tests
and artifacts. Separate code completion from actual source admission and any
remaining external release action. Identify release-critical prerequisites only;
no speculative feature expansion or extra architecture cycle. Return concise
implementation-ready recommendation with unresolved decisions explicit.
