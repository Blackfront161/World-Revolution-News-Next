# A2 M-001 correction — Root

Standing completion mandate; independent report at d4f8456a binds exactly one
Medium lifecycle finding. Root alone owns the existing production-media-player.ts
and apps/mobile/src/production-media-player.test.ts plus matching correction
evidence/handoff. No A1/A3/V1, dependencies, aliases, UI, provider or storage edits.
All reviewer rights returned. No delegation of writes and no children.

Capture/recheck epoch and liveness around authority/clock/connectivity callbacks
in preparation and confirmation. Recheck run identity and epoch after owns calls
authority; never let a stale event terminate a replacement run. Recheck current
online and post-callback ownership immediately before initial src and play.
Existing same-buffer continue while offline remains permitted. Add all five
independent witnesses as discriminating regression tests before fixing source;
also cover clock/initial-play boundaries where the same cause is reachable.
Run focused tests, both client types, scoped lint/format, boundaries and five
protected V1 pins. Preserve immutable RED evidence. Same independent reviewer
closes the finding after a separate candidate. No client activation implied.
