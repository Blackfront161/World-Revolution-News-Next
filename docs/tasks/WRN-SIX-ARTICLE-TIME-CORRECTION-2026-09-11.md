# Six-article Website generation time correction

Bound continuation of the PO completion mandate. Root alone owns this correction
after the independent review returned all rights. Finding
SIX-ARTICLE-INDEPENDENT-L-001 is accepted. No new architecture or content policy.

Allowed: the two activation helpers prepare-clients.mjs/finish-packaging.mjs;
the generated Website public website-publication.json for the active six-article
revision; new matching correction helpers/evidence/handoff, and Chief status.
No Mobile/public pointer, article, admission, descriptor, source time, native,
translation, UI, dependency, provider or old artifact changes.

Capture actual UTC once before generation, journal it, pass it consistently into
publication and packaging. The existing failed Website publication is preserved
byte-for-byte in the original immutable build and in the new correction journal.
Regenerate the local, never-published Website metadata from the unchanged core
release, with an explicit before-hash check; do not manually edit generated JSON.
Its content revision/sequence remain 3 because the admitted content is unchanged
and publication time is separate Website build metadata. This narrow local
replacement does not authorize mutation of released immutable content.

Build a fresh Website output, static landings, offline graph and verified package.
Prove all content bytes and client JS/CSS remain unchanged, all three generated
times equal the journal and precede completion, six landings/canonical/header
profiles remain correct, and the shell fits 8MiB. Preserve previous build/APK/PNGs.
Add a distinguishing chronology oracle that rejects the old future date.
No visual implementation changes: bounded exact bundle/content equivalence plus
one fresh Website offline/image case is sufficient; no repeated Mobile matrix.
Independent Sol closes this exact finding after the frozen correction candidate.

Root may work alongside the separately reserved read-only media design reviewer.
No competing product writer. No installs, signing, publishing or external writes.
