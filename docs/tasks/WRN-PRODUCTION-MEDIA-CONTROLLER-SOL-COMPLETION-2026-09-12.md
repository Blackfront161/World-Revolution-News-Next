# Complete the preserved A7 controller

Delegation erlaubt. Terra returned all rights and no running processes; its
incomplete eight-path checkpoint4f09855bd6909cc35f7fa3d8fc840d4c7befd849 is
preserved without acceptance. After this separate gate existing Sol
media_durability_completion alone owns the exact eight A7 paths from
CONTROLLER-AMENDMENT/IMPLEMENTATION. Root selection8f8beb85 is independently
GREEN, seven paths immutable. No second product writer, children, index, A1-A6,
aliases, dependencies, UI, provider, native or external action.

Own evidence: docs/evidence/WRN-PRODUCTION-MEDIA-CONTROLLER-SOL-COMPLETION-
2026-09-12/ and matching docs/handoffs/WRN-PRODUCTION-MEDIA-CONTROLLER-SOL-
COMPLETION-2026-09-12.md. Preserve Terra's original WIP report/handoff. You are
not alone; another Sol holds only first-boot design files. Root owns metadata
and accepted UI preview setup, no A7 writes.43173-75 transfer to this Sol;
user43192/93 and older43190/91 remain untouched. Root may open new immutable
UI8f8beb85 previews43194/95 separately, no shared testbrowser.

Binding authority: original controller design with ALL amendments94e4b4d1 and
writer729c0840 requirements, including declared duration/cancelConsent. No further
architecture approval is needed for the already bound missing behavior. Goal is
complete implementation with all15groups plus six finding oracles, not another
partial test-count report. Preserve genuine Ready identity and all A1-A6 gates.

Root observed actual gaps in the WIP, now partly corrected but not adequately
tested: command key hashes outside total deadline; clock fallback0/regression;
stale catches/observer callbacks overwriting a newer or disposed epoch; abandoned
late handles; A5 no-op cleanup ownership; continue deleting unowned records;
unbounded save/cleanup; dispose closing before owned drain; partial clear claiming
empty state despite residual rows; prepareResume accepting a requested stale row
when some different episode has a matching row. Do not assume initial fixes are
complete. Verify store lifetime AbortSignals across real mount/recheck/clear,
so a new command cannot reuse a handle closed by the previous signal. Dispose
must remain public-synchronous and final even after late completions.

Implement the missing tests as distinguishing success/negative oracles and use
them to finish the code. Controller-core matrix stays in Mobile standard graph.
Browser must use real A4/A6 and real A2/A5 for successful installation, failed
safety readback, exact pause save/reopen/resume, completed and pending continue,
concurrent newer rows and partial-clear truth. Fake Audio only; zero provider
requests. Deadline29999/30000/30001, stalled hash/open, clock throws/regression,
reentrant clear/dispose, no-op ownership, bounded cleanup/drain, and resume exact
episode identity are mandatory. No project-skip multiplication as coverage.

First targeted tests; after stable focused matrix, run full Mobile+Website once
(Root UI58affected/9copy/4Chrome/2types/builds already independently PASS), then
both types/static/aliases/boundaries and complete actual Chrome matrix with fresh
run outputs. Use direct bundled Node tools; do not run pnpm install or change
workspace settings because of the known pnpm configuration mismatch. Any other
path need or actual API impossibility must be surfaced before a write. Root
freezes the complete returned candidate for independent QA; no overall release
or provider/UI acceptance is granted by this implementation.
