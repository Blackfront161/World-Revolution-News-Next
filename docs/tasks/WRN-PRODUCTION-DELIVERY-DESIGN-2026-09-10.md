# Bounded production delivery design

Parent RELEASE-COMPLETION, explicit PO instruction to finish all release gaps.
Delegation: erlaubt, no children. Root reserves existing Slot2
production_content_design (Sol/high) for one bounded read-only pass; no product,
browser, index, provider configuration, secret discovery or remote writes.
Slot1 UI implementation and Root Events/Media integration are disjoint.

Two accepted real articles prove the reader pipeline, not ongoing delivery.
Determine the smallest concrete continuation from current production contracts,
publisher, exact client profiles and authoritative legacy source configuration
to a refreshable production service. In particular assess whether the Mobile
same-origin profile on Capacitor only re-reads bundled files; identify the exact
trusted-origin/transport boundary required for actual updates. Website and
Mobile stores remain separate. Use Source-of-Truth before legacy reads, do not
read credentials or assume a writable content-repository checkout.

Return a concise implementable contract: exact current files, new narrow files,
safe static publication/revision ledger inputs and deterministic outputs,
first-install bootstrap plus refresh behavior, fixed endpoint selection,
failure/offline preservation and targeted tests. Separate code/preparation that
can be finished locally from the final concrete deployment approval. Do not
claim that historic metadata, two admitted articles or an inert mock publisher
are a running feed. Avoid proposing a new backend if the existing static
contract can suffice. Reuse already completed design and validation; no broad
architecture or content-rights re-audit. One pass, then actionable handoff.

Own output only docs/evidence/WRN-PRODUCTION-DELIVERY-DESIGN-2026-09-10.md and
matching docs/handoffs/ file, template format, actual evidence and open bounds.
No implementation, install, signing, version bump, upload, paid API or PO
acceptance claim. END-CHECK: :)
