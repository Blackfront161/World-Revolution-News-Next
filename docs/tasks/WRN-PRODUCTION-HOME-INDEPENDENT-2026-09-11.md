# Independent production Home completion check

Parent: WRN-PRODUCTION-HOME-COMPLETION Stage2 and RELEASE-COMPLETION.
Delegation: erlaubt, no children. Start only after the Home writer returns all
rights, Root reproduces its candidate and a separate immutable candidate is
committed and identified in the dispatch. This document alone starts no agent.

Then Root may reserve an existing qa_release_engineer Terra/high in Slot1,
or a fresh instance if the earlier source reviewer is absent from live runtime,
read-only product/test and exclusive test browser43173/74/75/77/78. Preserve
43176/79/80 and emulator5558. Both design agents have now returned all rights.
Own only new HOME-INDEPENDENT evidence/handoff dated2026-09-11 and an optional
same-named output directory. No product/test/index/dependency/native writes.

Verify the exact13path Home diff and the writer/Root evidence, especially:
- actual two admitted articles/one image, one lead/up to5main/further split;
- real labelled non-live sport notes and up to5 archive references in both
  production clients, endpoint source preferences applied before archive cap;
- independent source/race/clear/saved behavior from the closed integration
  survives the new Home card reuse; original fixture Home stays untouched;
- lead image comes only from guarded admitted V2 blocks through the unchanged
  image renderer; hide/expiry/revocation/route changes cannot retain it;
- mixed article/image license links work only for the exact freshly checked
  article, with original external dialog/identity/URL/focus protections;
- asynchronous directory lifecycle handles failure/retry/abort and late results
  without leaking an obsolete loader's data or issuing repeated loads on filters;
- nine copy languages, accessible heading order, no overflow at320/390/1200,
  both required themes, RU200%, keyboard/forced colors and full Website offline.

Reproduce new focused Home/copy tests and the explicitly selected Home browser
spec with --project=mobile-390x844 --workers=2, absolute production origins.
Run other tests only for an unresolved concrete concern; Root owns full units,
source14case regression and updated-source protocol spec. Verify retained PNG
hashmanifest and inspect representative narrow/desktop/RU screenshots directly.
No claim based merely on a file existing or passing TypeScript. Distinguish
intentionally intercepted network failures from actual escaped requests/errors.

Report actionable severity/findings or bounded GREEN, commands/counts, screenshots
inspected, untouched data/core/fixture/reader image pins, limitations and rights
return. Do not infer editorial curation, current sports feeds, Apache runtime,
native packaging, whole roadmap or release acceptance. Handoff follows template
and ends WRN-AGENT-STATUS / END-CHECK: :).
