# V3 bounded browser transport and continuity

Delegation: erlaubt. After separate gate Root reserves Slot2 existing Terra
production_capacity_core_writer, no children/index/browser/network/install.
You are not alone; preserve others' edits. Root owns local artifact preparation;
Sol reviews publisher d34c0cdc independently. No shared UI or publisher writes.

Accepted capacity design and core b828ce63 remain binding. Own exactly:
- packages/browser-content/src/production-content-release.ts
- packages/content-contracts/src/production-content-release-v3.ts ONLY a frozen
  readonly resource-cap constant referencing the existing V3 policy. This makes
  limits available through the existing public export, without duplicating policy
  or exporting a configurable validation entry point. No validator modification.
- apps/mobile/src/production-content-release.test.ts
- apps/mobile/src/production-content-offline-controller.test.ts
- new apps/mobile/src/production-capacity-test-data.ts, authored test helper only.
Own new CAPACITY-TRANSPORT-WRITER evidence/handoff/output.

Select resource caps only after compatible descriptor validation. V3 articles/
admission384KiB, discover/archive128KiB, reader2816KiB; V1/V2 payload512KiB
(existing archive transport256KiB may stay stricter). Pointer32KiB, descriptor
128KiB and manifest256KiB remain fixed. No four independent4MiB payload fetches.
Preserve immutable captured source/receipt, safety before remaining payload,
abort cancellation and complete official validation. No raw legacyfeed fetch.
Store schema, controller product, timestamps/TTL, 3slots/12MiB, Clear/rollback
and highestAcceptedSequence invariants remain immutable.

Tests first show old4MiB arguments RED. Add authored valid V3 packet with >3
articles and actual >512KiB reader under limits, exact cap arguments and actual
oversize transport refusal before parse, unknown descriptor refuses all payload,
V1/V2 unchanged readiness and old V1 validator rejects V3. Use official compatible
factories and canonical receipts; source helper must not be production imported.
Exercise real controller safety barrier and pending Clear/expiry races with V3.
Required version continuity V2 -> V3 -> higher-sequence V2 emergency -> higher V3;
rollback/Clear must not lower sequence or restore revoked articles. If meaningful
full store proof needs browser, return completed unit candidate and precise
browser plan to Root; do not fake it with a stub that supplies expected results.

Run focused unit suites, full Mobile once, Mobile/Website/content-contract types,
scoped ESLint/Prettier and real boundary scan. Direct installed binaries; no pnpm
workspace install or bypass. No active public data/UI/store/fixture or provider
change. Return coherent result, exact files/tests/hashes, no commit, END-CHECK: :).
