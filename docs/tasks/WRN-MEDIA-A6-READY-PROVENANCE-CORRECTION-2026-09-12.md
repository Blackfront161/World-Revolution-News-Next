# Preserve genuine Ready at the A6 to A2 boundary

Delegation: erlaubt. Root found and independent Sol confirmed readActive loses
A1 WeakSet provenance through the store's JSON-cloning frozen() helper. A2
rejects every such stored context. Bind A6-READY-H-001 OPEN. Origin correction
908f7b50 remains frozen; its independent run is deferred to the combined closure.

After this separate gate existing Sol writer media_durability_completion owns:

- packages/browser-content/src/production-media-offline-store.ts
- tests/e2e/production-media-offline-store.spec.ts
- tests/e2e/production-media-offline-harness.ts only if required for the seam
- own docs/evidence/WRN-MEDIA-A6-READY-PROVENANCE-CORRECTION-2026-09-12 and handoff

All other product/tests/A1/A2/A4/A5/aliases/provider/UI/native/index OUT. No
children. You are not alone; preserve other edits. Exclusive blank43173-75,
independent Sol confirmed no active browser; Root no competing build/test load.

Correct only the readActive return boundary: preserve the exact already deeply
frozen A1 Ready object while freezing the returned scalar-metadata wrapper.
Do not change A1 provenance checks or weaken A2 admission. No Ready persistence.

Before changing the source, add and run a distinguishing real A6→A1→A2 browser
case: stage accepted fixture into actual IndexedDB, close/reopen, readActive,
assert A1 registry membership and nested immutability, and feed that exact object
into actual A2. A2 prepareConsent must produce its real prompt with zero audio
before confirm; explicit confirm uses a fake injected Audio and must reach its
play path without an external request. Include rollback A/B/A and verify fresh
Ready after rollback, plus rejection of a cloned/unregistered object by A2.
Keep fake audio only; never contact the fixture URL/provider. Preserve exact old
source failing output, then same oracle positive, full bound A6 Chrome matrix,
affected types and scoped static/boundaries. No broad repeated contract suite
unless a changed contract or actual failure makes it necessary.

Return exact three-file-or-narrower diff and complete results. Root freezes;
same independent Sol later closes A6-IND-M-001 and A6-READY-H-001 together after
the ongoing read-only controller design precheck. No independent self-closure.
