# V3 deterministic publisher and static-package integration

Continuation after Sol's exact structure diagnosis and Root input correction:
use candidate-1789065672080/production-build-input-v3.json in the same input
evidence directory, SHA d61e33e15f4dda44e1774bbbae8324588e5b8d1e116309cc0d465425450dd8b9.
Only the reviewed C4SS printed URL display and matching transformation/receipts
changed. Old input below is historical and must remain preserved. Sol returned
all diagnosis rights. Same Terra Slot2 resumes exactly this publisher scope;
finish remaining authored/boundary/delivery/site tests, not merely old4tests.

Delegation: erlaubt. After this separate gate Root reserves returned
production_capacity_core_writer Terra Slot2; no children/index/browser/network,
dependencies, live publication or activation. Coreb828ce63 independently GREEN,
single count finding and original-byte evidence gaps closed by sameSol.
Root writes translation UI/client only and freezes tools/browser-content-aliases.mjs
while this writer owns tools. No shared browser-content or client-source writes
in this publisher slice; transport follows after shared UI ownership returns.

Own exactly tools/build-production-content-release.mjs, its existing .test.mjs
and -v2.test.mjs, new tools/build-production-content-release-v3.test.mjs;
apps/website/tools/production-content-release.mjs and .test.mjs;
tools/prepare-production-content-delivery.test.mjs and
apps/website/tools/production-site-package.test.mjs. Own new
CAPACITY-PUBLISHER-WRITER evidence/handoff/output dated2026-09-11.
All other product/test files, contracts, old admitted inputs and old generated
directories are immutable. If an actual additional product defect prevents
completion, report the exact needed path and cause; do not silently expand scope.

Add build-input.v3 dispatch, descriptor/manifest3.0.0 and V2 reader semantics,
using the validated compatible factory. Preserve V1/V2 output determinism and
limits byte-for-byte. Select static loader resource caps only after a validated
versioned descriptor; unknown versions fail before payload. Never feed the
27MiB upstream JSON to clients or raise every fetch/file allowance to4MiB.
Preserve deterministic ordering, immutable files and current.json pointer-last
protocol, canonical website publication equality and site8MiB cap.

Run existing4builder cases plus authored6/8/64 V3 generation twice with equal
byte/hash manifests, 65 rejection and invalid/oversize versions. Add actual
static-loader V3 reader payload >512KiB but <=V3cap, at/above caps and unchanged
V1/V2 refusals. Delivery and sitepackage should accept/reject V3 through their
existing protocol; include a deterministic V3 publication/8MiB budget case.
No public/current source activation, no replacing client/public or prior previews.

Exercise the independently reviewed Root real input only into a fresh local
output directory after unit success:
docs/evidence/WRN-NEWS-SIX-ARTICLE-INPUT-2026-09-11/candidate-1789062115842/production-build-input-v3.json
SHA b501bd2a91f063fc8bc9454b3b96e0d900f7f1a37a301db4773892909aba3abb.
Verify6 admitted articles/4 images, full source receipts and old2record continuity.
Produce a complete local V3 release and website publication if existing scoped
tools permit, then return exact paths/hashes for Root's later client activation.

Use direct installed Node binaries (pnpm workspace state awaits reconciliation;
no install or disabling dependency validation). Relevant Node suites/types/scoped
format/lint/boundary then one coherent candidate. Independent QA follows once;
finish bounded publisher scope, preserve others' edits, and END-CHECK: :).
