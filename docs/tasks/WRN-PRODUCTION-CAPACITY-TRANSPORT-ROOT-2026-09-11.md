# Root completion of bounded transport

Terra returned two incomplete checkpoints: direct binaries finally20oldtestsPASS,
but none of the required authored V3 oracles was delivered. Root preserves the
two-path product WIP and writer evidence in the preceding commit. Slot2 and all
writer rights returned; no new writer or retry. Chief takes exactly the five
paths in CAPACITY-TRANSPORT after this separate gate. No validator/store/UI change.
Sol continues independent publisher closure, disjoint and read-only.

Root adds a valid authored versioned helper using existing official fixture and
canonical admission/manifest factories; first proves V3 readiness/counts. Then
old4MiB cap RED, actual byte bounds and unknown-version pre-payload rejection,
real controller durable-safety wait and cancellation. Full real IndexedDB mixed
V2/V3 sequence/Clear/rollback/expiry proof in own new evidence-only harness on
43185/86. Own CAPACITY-TRANSPORT-ROOT evidence/handoff/output. No claims from
stubbed store success. Existing actual IDB store remains unchanged.

Both this completion and publisher independent GREEN precede six-article public
activation. Same task's fullMobile/type/static checks and narrow independent
closure remain binding. Source/helper tests can run with direct installed
binaries; pnpm dependency-state warning does not prevent them. No install,
provider, sign, delete, deploy or existing-preview replacement.
