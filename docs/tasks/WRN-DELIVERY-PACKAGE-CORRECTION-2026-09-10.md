# Delivery package correction

Parent RELEASE-COMPLETION, candidate108a833, independent Sol review H001 and
M002–M004. Delegation: erlaubt; Slot1 existing production_mobile_b1 Terra/high
sole writer of tools/prepare-production-content-delivery.mjs and its test plus
matching WRN-DELIVERY-PACKAGE-CORRECTION-2026-09-10-WRITER reports. No children,
index, browser, other sources or external actions. Root concurrently owns the
disjoint Website personalization client package. Slot2 review first returns
rights; then this committed correction brief permits the two-file fix.

H001: Canonicalize the trusted workspace; inspect each existing segment from
its real root through input, ledger, output parent and fresh staging. Reject
symbolic links/junction/reparse aliases and paths outside the actual root.
Require existing regular parent directories before creating staging. Bind and
recheck exact output boundary before final promotion; preserve caller targets.
No deletes/cleanup. Fresh Windows directory junctions work in this environment:
test input, ledger and output-parent rejection plus absent outside output.
Keep the earlier unsupported file-symlink diagnostic honest; no skipped ancestor
oracle. Roots with conventional OS ancestors are not arbitrary admitted aliases.

M002: Write the already validated/read bytes with writeFile(flag:'wx') so hashes
describe exactly the emitted bytes and collisions fail. A narrow injected
before-copy hook may create one collision for testing; no broad mutation API,
filesystem override, overwrite or network. Prove existing bytes remain intact
and no ready package is returned on the collision. Recheck before final rename
with absent target, no automatic replacing/moving historical files.

M003: Every supplied ledger begins at sequence1. previousLedgerSha256 is null
iff entries.length===1; longer chains require SHA256. Preserve all cumulative
safety and exact existing constraints. Distinguishing negative cases [5]->6,
one-entry-with-hash and multi-entry-with-null all fail without ready output.

M004: One strict CLI parse: exact known options, every value option once,
no duplicate or positional/unknown tokens, no flag-as-value, exactly one of
--genesis or --ledger value. Test success from non-root cwd and each malformed
argument case with nonzero exit/no output. No implicit cwd or genesis fallback.

Preserve actual admitted input, deterministic two-run bytes/manifest/ledger,
higher-sequence followup, 512/4MiB caps and activation-last order. Run the full
tool test file and scoped lint/format/diff checks; retain fresh artifacts only.
Return complete handoff, not a partial patch. Root reproduces, then same Sol
reviewer closes only four findings. No Mobile/controller/UI/contract writes.
The independent reviewer owns only matching WRN-DELIVERY-PACKAGE-CORRECTION-
INDEPENDENT-2026-09-10 evidence/handoff and fresh narrow Node probes. No browser.
END-CHECK: :)
