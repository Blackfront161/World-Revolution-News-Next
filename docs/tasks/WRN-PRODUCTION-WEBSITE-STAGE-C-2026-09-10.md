# Website production content and publication

Parent RELEASE-COMPLETION. Approved content-design Stage C and prepared cut;
shared e84ebbd must close independent Terra/Sol before Website consumes it.
Delegation: allowed, no children. Root owns Website integration/UI; Slot1 may
own only the separate disk publication scope below after explicit reservation.
Source-of-Truth and individually admitted pilot output remain binding.

## User outcome and client boundaries

Default Website shows the admitted production articles, current/archive Reader,
save/read/manual position and updates with guarded external source/license links.
One shared library supplies exactly the accepted mechanics; Website owns
wrn.website-production-content-offline.v1 and wrn.website-production-reading-state.v2.
No v1 read/write/delete on default production path, no Mobile key or app import.
History, theme, language, support draft guard and Website shell panel stay local.
Production route labels use existing translations and actual original language.
Black/violet/red default remains; selected controls red filled, other theme
choices retained. No fixture controls or preview footnotes on production paths.

## Root exact source/test scope

apps/website/src/App.tsx and App.test.tsx; new thin production-content-offline-
profile/store/controller, production-reading-state, production-content-ui.tsx;
new focused production adapter/component tests. Existing shared renderer used
through a trusted Website factory, heading ID website-page-title and archive
trigger website-more-archive. Keep accessible Website H1 and content H2 hierarchy.
App contentMode fixture-offline is explicit component/test injection only; query,
storage and default production builds cannot select fixtures. Existing two
default-fixture unit cases gain explicit injection, same assertions retained.
The subsequent source pass finds three such cases including shareAdapter-only
archive opening; all three retain their assertions with explicit fixture mode.

apps/website/package.json build/test script wiring; tests/e2e/global-setup.ts;
new production-website-content.spec.ts and narrow authored harness if needed.
Global setup keeps genuine default Website static output on a new43178 server;
old43174 strict fixture acceptance uses a fresh separate test build with explicit
entry transform (same pattern as Mobile), no hidden production switch.43175
source harness remains for explicit test imports. Preserve43176 preview. New
Vite builds use unique output/cache paths. No old fixture tests weakened.
Two old fixture acceptance readers (website-shell-built.spec.ts and
website-shell-core-helper.mjs) also consume the explicit fresh fixture output
handed down by globalSetup, instead of accidentally reading production dist.
This is a test-only output path, never an application mode switch. Assertions
and the eight-entry closed graph remain unchanged.

Root may add named exports for existing HTML/XML/JSON escaping and text block
renderer in generate-static-article-landings.mjs for publisher reuse; existing
fixture behavior stays intact. Root integrates defaults only after publisher
handoff. Website shell protocol/asset graph remains unchanged at eight entries;
production JSON/static landings are separate content/publication output, not
offline shell entries. Events/Media graph extension is a separate next slice.

## Separate disk publication writer scope

New files under apps/website/tools: production-content-release.mjs/.test.mjs,
build-production-website-publication.mjs/.test.mjs,
generate-production-article-landings.mjs/.test.mjs,
integrate-production-article-landings.mjs/.test.mjs. May own generated9files
under apps/website/public/wrn-production-content and its own evidence/handoff.
No existing generator/integrator/package/App/global-setup/shared writes by worker.

Read the eight admitted core files from
docs/evidence/WRN-PRODUCTION-ARTICLE-PILOT-2026-09-10/generated-core-e8506a4;
copy exact bytes via deterministic local producer into an absent Website
production directory, create a separate ProductionWebsitePublicationV1 binding
exact manifest hash/revision and complete article/landing/sitemap ID sets.
siteOrigin canonical https://solinaridao.com/, fixed generator version, generatedAt as an
explicit immutable Website publication input; no wall-clock/network/API
dependencies inside the producer. The admitted descriptor has no generatedAt:
do not extend it or mislabel source-check time. Chief clock observed
2026-09-10 06:51:40 UTC binds this projection to2026-09-10T06:51:40.000Z.
API requires this timestamp input and rejects missing/invalid values. No hand-edited
publisher text or fabricated rights. Refuse existing targets unchanged.

Disk loader resolves only derived fixed same-origin paths within explicit trusted
root, enforces per-file/total bounds and fatal UTF8/JSON, pointer-descriptor hash
and exact release validation, then independent publication binding. No fallback
to fixtures or source URLs. Static renderer reuses existing escaping/text-block
helpers, adds authors/source/date/original language/completeness/license/evidence/
transformation, and escaped NewsArticle JSON-LD. Use existing approved black,
violet/red semantic values; no provider request/scripts/artwork/embeds. Interactive
reader links remain /?article=wrn-art-...; canonical URL fixed approved origin.
Static landings contain admitted text, never test-publication wording.

Local publisher writes only a newly reserved absent target, manifests exact HTML,
sitemap/robots hashes and complete IDs. Integration checks absence of all four
artifact destinations before promoting; injected mid-promotion failure must
restore original caller files. Preserve failed staging/evidence rather than
deleting files. No live publication or production cache mutation.

## Acceptance and return

All existing117Website units and36tools plus new disk/publisher tests; four shared
block types/current+archive, two real admitted articles, safe alias/gone, source
consent/no request, actual reload/save/progress, per-client v1/v2 isolation,
expiry/revocation/phase-two failure/clear/rollback/browser guards. Test canonical
landing navigation, exact file/hash/set correspondence and wrong path/hash/set,
unsafe attribution/HTML/JSON boundaries, oversize input and existing-target
preservation; no success from empty data. Generate twice and compare bytes.
Real Website browser at320/390/1440, RU200, violet/dark/contrast, all9languages,
keyboard focus/history and Axe. Fresh image/hash evidence. Website closed shell
build plus production public assets stay separated, both clients still build.
Freeze candidate, independent Terra QA and Sol narrow data/publication integrity;
no new architectural round without a concrete contradiction. Rollback is the
isolated code candidate; original fixture stores and immutable pilot preserved.
Own reports WRN-PRODUCTION-WEBSITE-STAGE-C and -PUBLICATION, END-CHECK: :).

Native recopy/final hygiene/real devices/upgrade and release approval remain
separate parent work. No installation, signing, version code, paid API, push,
deployment or PO visual acceptance is authorized or claimed by this slice.
